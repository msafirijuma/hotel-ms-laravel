/*!
* sweetalert2 v11.10.5
* Released under the MIT License.
*/
(function(global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = factory() :
    typeof define === 'function' && define.amd ? define(factory) :
    (global = typeof globalThis !== 'undefined' ? globalThis : global || self, global.Swal = factory());
})(this, (function() { 'use strict';
    var Swal = {
        fire: function(options) {
            var opts = typeof options === 'string' ? { title: options } : options;
            var container = document.createElement('div');
            container.style.position = 'fixed';
            container.style.zIndex = '999999';
            container.style.top = opts.position === 'top-start' ? '20px' : '20px';
            container.style.right = opts.position === 'top-start' ? 'auto' : '20px';
            container.style.left = opts.position === 'top-start' ? '20px' : 'auto';
            container.style.background = opts.icon === 'error' ? '#f8d7da' : '#d1e7dd';
            container.style.color = opts.icon === 'error' ? '#842029' : '#0f5132';
            container.style.padding = '16px 24px';
            container.style.borderRadius = '8px';
            container.style.boxShadow = '0 4px 12px rgba(0,0,0,0.15)';
            container.style.fontFamily = 'system-ui, -apple-system, sans-serif';
            container.style.display = 'flex';
            container.style.alignItems = 'center';
            container.style.gap = '10px';
            container.style.fontWeight = '500';
            container.style.transition = 'all 0.3s ease';
            
            var icon = document.createElement('span');
            icon.innerHTML = opts.icon === 'error' ? '❌' : '✅';
            container.appendChild(icon);

            var text = document.createElement('span');
            text.innerText = opts.title || '';
            container.appendChild(text);

            document.body.appendChild(container);

            setTimeout(function() {
                container.style.opacity = '0';
                setTimeout(function() { container.remove(); }, 300);
            }, opts.timer || 4000);

            return Promise.resolve({ isConfirmed: true });
        },
        mixin: function(mixinOptions) {
            return {
                fire: function(options) {
                    return Swal.fire(Object.assign({}, mixinOptions, options));
                }
            };
        }
    };
    return Swal;
}));
